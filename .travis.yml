os: linux

dist: xenail

language: c

compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - g++-6

install:
  cd ci/travis && ./configure && make && make install

before_script:
  - cd -
  - pip install --user cpp-coveralls

script:
  - make
  - ./test_all.out

after_success:
  - coveralls --root ${PWD} --include ci/coveralls/src/  --gcov-options '\-lp'
