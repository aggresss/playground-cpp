language: c
compiler: gcc
#addons:
#  apt:
#    sources:
#      - ubuntu-toolchain-r-test
#    packages:
#      - g++-6
install:
  cd ci/travis && ./configure && make && make install
script:
  - echo "script begin"
  - if [ ! -f hello-release.out ]; then
        exit 127;
    fi
  - echo "script finish"

